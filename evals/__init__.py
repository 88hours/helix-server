"""
Helix eval suite — validates agent output quality using LangSmith.

Modules:
    datasets    — sample crash events and QA inputs used as eval examples
    evaluators  — scoring functions that assess agent output quality
    run         — CLI runner: uploads datasets and executes evals via LangSmith
"""
